{
  "name": "understand-x",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev -w www\" \"npm run dev -w api\" \"npm run worker:dev -w api\"",
    "build:shared": "npm run build -w @understand-x/shared",
    "build": "npm run build:shared && npm run build -w www && npm run build -w api"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "overrides": {
    "prisma": "5.22.0",
    "@prisma/client": "5.22.0"
  }
}
